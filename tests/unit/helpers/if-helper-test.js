import { ifHelper } from 'tellme/helpers/if-helper';
import { module, test } from 'qunit';

module('Unit | Helper | if helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifHelper([42]);
  assert.ok(result);
});
