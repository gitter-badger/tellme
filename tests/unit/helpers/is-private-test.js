import { isPrivate } from 'tellme/helpers/is-private';
import { module, test } from 'qunit';

module('Unit | Helper | is private');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isPrivate([42]);
  assert.ok(result);
});
